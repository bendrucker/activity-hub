name: Deploy

on:
  push:
    branches: [main]

concurrency:
  group: deploy

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
      - run: bun install --frozen-lockfile
      # ci.yml also runs on push to main but nothing orders the two
      # workflows, so deploy re-runs the checks itself before touching
      # production.
      - run: bun run typecheck
      - run: bun run test
      - name: Apply D1 migrations
        run: bun run wrangler d1 migrations apply activity-hub-registry --remote
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      - name: Deploy worker
        run: bun run wrangler deploy
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
