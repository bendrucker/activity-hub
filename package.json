{
  "name": "activity-hub",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit && tsc -p scripts --noEmit",
    "import": "bun scripts/import.ts",
    "test": "vitest run",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "types": "wrangler types",
    "dev": "wrangler dev",
    "wrangler": "wrangler"
  },
  "dependencies": {
    "@garmin/fitsdk": "^21.208.0",
    "@mapbox/polyline": "^1.2.1",
    "csv-parse": "^7.0.1",
    "tz-lookup": "^6.1.25",
    "ulid": "^3.0.2"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.18.0",
    "@types/bun": "^1.3.14",
    "@types/mapbox__polyline": "^1.0.5",
    "@types/tz-lookup": "^6.1.2",
    "prettier": "^3.9.5",
    "typescript": "^7.0.2",
    "vitest": "^4.1.0",
    "wrangler": "^4.111.0"
  }
}
